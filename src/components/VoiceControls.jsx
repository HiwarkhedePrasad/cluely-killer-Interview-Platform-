import React from 'react';

/**
 * Voice Controls Component
 * Voice input/output is handled automatically by the interview system.
 * This component is intentionally minimal — AI agents speak and the
 * interview system listens without user-managed controls.
 */
export function VoiceControls({ className = '' }) {
  return null;
}

export default VoiceControls;
